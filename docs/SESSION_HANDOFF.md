# Session Handoff — Phase 3.5 complete (start here in a new session)

This file is the briefing for a fresh Claude Code session picking up the
Vantor Capital Markets project after Phase 3.5 (Private Beta Readiness,
on top of Phase 3 Investor Experience + Watchlists + Discovery
Intelligence). Read this first, then
`VANTOR_HANDOFF.md` (per-session detail) and `docs/DEPLOYMENT.md`
(operational runbook) as needed.

## Project in one paragraph

Vantor Capital Markets is a private-markets platform: founders onboard
companies, enter metrics, request verification, and receive deterministic
valuations; investors browse published companies, search/filter/sort the
marketplace, and keep a persisted private watchlist; admins review
submissions and verification evidence. Stack: Next.js 16.3.1 App Router
(breaking changes vs. training data — read `node_modules/next/dist/docs/`
before writing Next code, per `AGENTS.md`), React 19, TypeScript strict,
Tailwind v4, Drizzle ORM + postgres-js on PostgreSQL 16, better-auth
^1.6.29. Deployed on Vercel (app) + Railway (Postgres only). Migrations
run from a dev machine, never at serverless boot.

## Current state

- **Branch:** `claude/vantor-phase-3-investor-2mpd9m` — Phase 3 work.
  Based on the stabilization branch tip (`4a94263`, branch
  `claude/vantor-stabilization-qa-m4kne0`), which was never merged into
  the default branch (`claude/vantor-platform-foundation-uw0r8u`); this
  branch carries all of it. Never push to other branches; never merge
  without Jack's say-so; never deploy over Production.
- **Validation at head:** lint clean, typecheck clean (after a build —
  see caveat below), 122/122 tests, production build passing.
- **What Phase 3.5 added:** demo/real separation (Demo chips everywhere
  demo companies render; demo hidden entirely on the production
  deployment), unit-tested seed guard, admin "recently edited published
  companies" view, metric help text + currency guidance, /feedback +
  /admin/feedback, product_events usage counts on the admin dashboard,
  mobile QA fixes. Beta access decision: open signup, publication review
  is the gate (documented in VANTOR_HANDOFF.md).
- **What Phase 3 added:** watchlists (schema was pre-staged; now live end
  to end), Discover sorting (5 whitelisted sorts incl. SQL-side latest
  revenue/growth/valuation), discovery signal chips (New to Vantor /
  Recently updated / Highly verified), Save controls on Discover rows and
  company profiles, a `/watchlist` page, Watchlist nav entry,
  `/login?next=` return-path support, richer seeds (recency variance +
  verification requests). Full detail in `VANTOR_HANDOFF.md`.

## Manual provider-side steps — REQUIRED before/at Preview review

1. **Apply migrations 0004 + 0005** (`0004_…` index;
   `0005_dusty_landau.sql` adds the `feedback` and `product_events` tables)
   to BOTH Railway databases: `DATABASE_URL="<preview-url>?sslmode=require"
   pnpm db:migrate` and again with the production URL. Both are additive,
   no data movement.
2. **Optionally reseed Preview** (`ALLOW_SEED=true pnpm db:seed` against
   the Preview DB) if you want the new demo verification requests and
   publish/update recency variance there. The seed skips existing rows
   (idempotent by slug); for the full effect, reseed a clean database.
3. Vercel auto-deploys the branch to a Preview URL (behind Vercel SSO).
   Production admin path (`pnpm admin:grant`) is unchanged.

## Standing constraints (do not relax)

- Visual identity is LOCKED — the dark premium system is approved.
- Valuation V1 methodology is LOCKED (version-bump to change; never AI).
- Regulatory boundary: no transactions, orders, payments, share prices,
  investment advice, or recommendation language. Watchlists are research
  bookmarks, never portfolios.
- Watchlist privacy: every read/mutation is scoped server-side to the
  session user inside `src/db/queries/watchlists.ts` — keep it that way.
- Marketplace `sort` values must stay whitelisted (`MARKETPLACE_SORTS`).
- Never print or commit secrets; never seed production
  (`ALLOW_SEED` + NODE_ENV guards).

## Known caveats

- **Fresh-clone typecheck fails** before a build: `logo.tsx` PNG imports
  need `next-env.d.ts`, generated by `pnpm build` (or one `next dev`).
- The `AGENTS.md` block about Next.js docs is re-added by `next dev`;
  committing it keeps the tree clean.
- Tests need `TEST_DATABASE_URL` whose database name contains "test".
- In the sandboxed dev environment, dev-mode React logs a CSP `eval()`
  console error — dev-only artifact; production build consoles are clean.

## Where the next session probably goes

Jack's decision: the product is beta-ready — recruit the first founders
and investors, operate the beta through /admin, and let real usage decide
what Phase 4 (Company Intelligence) should contain. Do not start
regulated functionality or Valuation V2.

## Command reference

```bash
pnpm install
pnpm dev             # local dev
pnpm lint && pnpm typecheck
pnpm test            # 122 tests; needs TEST_DATABASE_URL
pnpm build           # production build (also generates next-env.d.ts)
pnpm db:generate     # new migration from schema changes
pnpm db:migrate      # apply migrations (DATABASE_URL)
pnpm db:seed         # demo data (ALLOW_SEED=true, non-production only)
pnpm admin:grant <email>
```
